# assets/

このゲームは正式な画像素材を持たず、**すべての仮素材を実行時に JavaScript で生成**します
（`src/scenes/BootScene.js` が Phaser の Graphics から `generateTexture` で作成）。

そのためこのフォルダは現状ほぼ空ですが、将来的に正式なスプライトシートや音声を追加する場合の
配置先として用意しています。GitHub Pages の公開対象にも含まれます（相対パス参照）。
